---

- name: using modular approach to build automation
  hosts: localhost
  vars:
    show_output: True
    write_to_file: True
  # roles:
  #   - role: servicenow
  #     tasks_from: fetch.yml
  tasks:

    - name: fetch tickets
      include_role:
        name: servicenow
        tasks_from: fetch.yml